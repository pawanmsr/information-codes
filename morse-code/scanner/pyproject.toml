[project]
name = "morse-code-analyser"
version = "0.1.0"
description = "Scanner and parser for morse code."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "antlr4-tools>=0.2.2",
]
