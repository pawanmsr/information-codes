Receiver
========

The Backend.

Instructions
------------

Mavan and Gradle (with later being primary) can be used to compile and build.

```shell
# Check dependency
mvn dependency:tree
gradle dependencies

# Run using gradle
gradle clean
gradle bootRun

# Run using maven
mvn clean install
mvn spring-boot:run

```
